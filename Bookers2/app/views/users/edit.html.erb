 <main>
  <% if @user.errors.any? %>
    <h3>
      <%= @user.errors.count %> error prohibited this obj from being
      <br>saved:
    </h3>
    <% @user.errors.full_messages.each do |message| %>
    <ul>
      <li><%= message %></li>
    </ul>
    <% end %>
  </div>
  <% end %>
<div class="container">
  <div class="row">
    <div class="col-sm-12 col-md-8 col-lg-5 px-sm-0 mx-auto">
  <h2>User info</h2>
  <div class="error_explanation">
  

  <%= form_with model: @user, url: user_path(@user.id), method: :patch do |f| %> 
  <div class="form-group">
    <laber for="user_name">Name</laber>
    <%= f.text_field :name %>
  </div>
  <div class="form-group">
    <laber for="user_image">Image</laber>
    <%= f.file_field :profile_image, accept: "image/*" %>
  </div>
  <div class="form-group">
    <laber for="user_introduction">Introduction</laber>
    <%= f.text_area :introduction %>
  </div>
  <div class="form-group">
    <%= f.submit "Update User", class: "btn btn-secondary" %>
  </div>
  <% end %>
  </div>
  </div>
  </div>
  </div>
  </main>